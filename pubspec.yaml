name: jocaagura_ia
description: >-
  A Dart package in development for local AI in Dart and Flutter applications,
  with Jocaagura contracts for offline inference and model management.
version: 0.0.0
repository: https://github.com/grupo-jocaagura/jocaagura_ia

environment:
  sdk: ^3.13.2

# Add regular dependencies here.
dependencies:
  # path: ^1.9.0

dev_dependencies:
  coverage: ^1.15.1
  lints: ^6.0.0
  test: ^1.25.6
