name: jocaagura_ia
description: >-
  Paquete Dart en desarrollo para integrar IA local en aplicaciones Dart y Flutter,
  con contratos Jocaagura para inferencia offline y gestión de modelos.
version: 0.0.0
repository: https://github.com/grupo-jocaagura/jocaagura_ia

environment:
  sdk: ^3.13.2

# Add regular dependencies here.
dependencies:
  # path: ^1.9.0

dev_dependencies:
  coverage: ^1.15.1
  lints: ^6.0.0
  test: ^1.25.6
