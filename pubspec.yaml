name: jocaagura_ai
description: >-
  Pure Dart domain contracts for local AI inference and model lifecycle management,
  with immutable JSON models and runtime-independent interfaces.
version: 0.1.0
repository: https://github.com/grupo-jocaagura/jocaagura_ia

environment:
  sdk: ^3.13.2

dev_dependencies:
  coverage: ^1.15.1
  lints: ^6.0.0
  test: ^1.25.6
